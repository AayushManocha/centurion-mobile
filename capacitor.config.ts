import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aayushmanocha',
  appName: 'centurion-mobile',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
